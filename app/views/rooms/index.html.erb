<%# CSS・・・assets/stylesheets/room.css %>
<%= render "share/header" %>
<div class='room-main'>
  <div class='room-detail'>

    <% current_user.rooms.each do |room| %>
    <div class='room-set'>
      <%= link_to messages_path, class: "link" do %>
      <div class='room-set-name'>
        <div class='room-name'><%= room.name %></div>
      </div>
      <% end %>
      <div class='room-set-member'>
        <div class='room-member'>
        <% room.users.where.not(id: current_user.id).each do |user| %>
        <div>
        <%= user.nickname %>
        </div>
        <% end %>
        </div>
      </div>
      <div class='room-set-delete'>
        <%= link_to room_path(room), method: :delete, class: "link" do %>
        <div class='room-delete'>削除</div>
        <% end %>
      </div>
    </div>
    <% end %>

  </div>
</div>

